api:
  enabled: true
  address: 0.0.0.0:8686

sources:
  nginx_logs:
    type: file
    include:
      - /var/log/nginx/access.log

sinks:

  kafka_topic:
    type: kafka
    inputs:
      - nginx_logs
    bootstrap_servers: "kafka:9092"
    topic: "kafka_topic"
    encoding:
      codec: json
